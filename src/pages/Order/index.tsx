import styled from '@emotion/styled';
import React from 'react';

const OrderPage: React.FC = () => {
  return (
    <Container>
      <h1>주문 페이지</h1>
      {/* 여기에 주문 페이지 내용을 추가합니다 */}
    </Container>
  );
};

export default OrderPage;

const Container = styled.div`
  padding: 20px;
`;
